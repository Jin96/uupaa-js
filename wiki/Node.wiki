#summary Node は Element の別名です。

= Node =

Node は Element の別名です。配列の要素(Array.Element)と区別する場合に便利です。

{{{
    var node = document.createElement("div");

    var nodeList = document.getElementsByTagName("div");

    // 「Arrayの各ElementにはNodeが格納されている」
    var array = [nodeList[0], nodeList[1]];
}}}